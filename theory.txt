1. 5 видов операторов
2. let date = new Date();
let time = date.getHours();
(time < 10) ? alert( "Доброе утро!" ) : alert( "Добрый день!" );
3. Выведется
4. true
false
true
5. - Использование атрибута в HTML разметке (к примеру, onclick = "alert('Клик!')")
- Использование свойства DOM-объекта (Обращение к индификатору элемента в скрипте: id.onclick = function() {}; или id.onclick = function;  )
- черезе this (в разметке обращаемся к элементу onclick="alert(this.innerHTML)")
- метод addEventListener (он позволяет добовлять несколько обработчиков на одно событие) (element.addEventListener(event, handler[, options]);)
6. Необходимо писать без () 
button.onclick = hello;
7. keydown и keyup– когда пользователь нажимает / отпускает клавишу
8. Когда происходит событие, браузер создаёт объект события И записывает в него детали, передаёт его в качестве аргумента функции-обработчику. к примеру координаты курсора мыши 
9. равно ==
больше/меньше <>
больше/меньше/равно >= <=
не равено !=